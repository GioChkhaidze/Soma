[package]
name = "soma-ai-runtime"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[lib]
name = "soma_ai_runtime"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ureq = { version = "2.12", features = ["json"] }
