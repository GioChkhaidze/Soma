# Archive

These files summarize early product and stack research. They are historical records.

They do not define current requirements, dependencies, or implementation plans.

Use these sources for current behavior:

- `docs/architecture/`
- `docs/decisions/`
- executable contracts and tests

The summaries preserve the important alternatives and rejected directions. They omit
repeated generated analysis and obsolete implementation instructions.
